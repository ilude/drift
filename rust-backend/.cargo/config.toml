# dlltool.exe is required by the GNU rustc toolchain when linking Windows import
# libraries. It is bundled in the rustup self-contained directory; ensure it is
# on PATH by copying it to ~/.cargo/bin (done once during workspace setup).
# No linker override needed — the default GNU ld handles everything once dlltool
# is resolvable.
